import{_ as d,c as o,o as c,ag as a}from"./chunks/framework.CldZrGrL.js";const u=JSON.parse('{"title":"Embedding 提供商","description":"","frontmatter":{},"headers":[],"relativePath":"guide/embedding.md","filePath":"guide/embedding.md"}'),i={name:"guide/embedding.md"};function n(t,e,m,l,r,s){return c(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="embedding-提供商" tabindex="-1">Embedding 提供商 <a class="header-anchor" href="#embedding-提供商" aria-label="Permalink to &quot;Embedding 提供商&quot;">​</a></h1><p>Milvus Tools 目前支持两种 Embedding 方案：</p><ul><li><code>ollama</code>：通过 <code>scripts/ingest/milvus-embed-ollama.mjs</code> 调用本地 Ollama HTTP 接口（默认 <code>http://127.0.0.1:11434</code>），并发度通过 <code>OLLAMA_CONCURRENCY</code> 控制。</li><li><code>mock</code>：使用 <code>scripts/ingest/milvus-embed-mock.mjs</code> 生成固定向量，便于测试或离线验证。</li></ul><h3 id="🎯-默认模型" tabindex="-1">🎯 默认模型 <a class="header-anchor" href="#🎯-默认模型" aria-label="Permalink to &quot;🎯 默认模型&quot;">​</a></h3><p>当前默认配置是 <code>EMBED_PROVIDER=ollama</code>，<code>OLLAMA_MODEL=nomic-embed-text</code>（768 维），并写在 <code>config.json.embedding.ollama</code> 中。只要 Ollama 启动并拉取了 <code>nomic-embed-text</code>，<code>pnpm run milvus:ingest</code> 会透明调用该模型；若要替换模型，需同步调整 <code>OLLAMA_MODEL</code> 与 <code>EMBEDDING_DIM</code>（确保维度一致），然后先运行 <code>pnpm run milvus:rebuild</code> 再 <code>pnpm run milvus:ingest</code>。</p><h3 id="🌐-修改策略" tabindex="-1">🌐 修改策略 <a class="header-anchor" href="#🌐-修改策略" aria-label="Permalink to &quot;🌐 修改策略&quot;">​</a></h3><p>要切换到 OpenAI 或其他 provider，把 <code>embedding.provider</code> 改为目标名称（如 <code>openai</code>），配置好对应的 <code>model</code>/<code>apiKey</code>，并保证 <code>EMBEDDING_DIM</code> 与模型维度匹配。<code>config.json.embedding</code> 提供 <code>provider</code>、<code>baseUrl</code>、<code>dimension</code>、<code>concurrency</code> 等字段，MCP 与 CLI 脚本会共享该配置。</p>',7)])])}const _=d(i,[["render",n]]);export{u as __pageData,_ as default};
